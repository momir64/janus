<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:tools="http://schemas.android.com/tools"
        xmlns:android="http://schemas.android.com/apk/res/android">

    <application
            android:dataExtractionRules="@xml/data_extraction_rules"
            tools:ignore="DataExtractionRules,CredentialDependency"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:theme="@style/Theme.Privezak"
            android:icon="@mipmap/ic_launcher"
            android:allowBackup="false"
            android:label="Privezak">
        <activity
                android:name=".MainActivity"
                android:exported="true"
                android:theme="@style/Theme.Privezak.Splash"
                android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
                android:name=".CredentialActivity"
                android:exported="false"
                android:excludeFromRecents="true"
                android:taskAffinity=""
                android:theme="@style/Theme.Privezak.Transparent"
                android:windowSoftInputMode="adjustResize" />

        <service
                android:name=".provider.PrivezakCredentialProviderService"
                android:exported="true"
                android:label="Privezak"
                android:permission="android.permission.BIND_CREDENTIAL_PROVIDER_SERVICE"
                tools:targetApi="34">
            <intent-filter>
                <action android:name="android.service.credentials.CredentialProviderService" />
            </intent-filter>

            <meta-data
                    android:name="android.credentials.provider"
                    android:resource="@xml/provider" />
        </service>
    </application>
</manifest>